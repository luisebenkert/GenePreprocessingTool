#!/usr/bin/python

#Script for including discretization strategies in data preprocessing. Currently, this is a placeholder for upcoming implementations
import sys
import os, stat, subprocess

SCRIPT_MEAN =""" """ #todo: apply discretization strategy

def discretize(scriptfile, datasetid, approach):
    
    #if approach == "mean":
    #    scriptfile.write(SCRIPT_MEAN.format(datasetid) + "\n")
